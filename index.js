'use strict'

// This represents the application object. Require
// Follows the path and retrieves the module from
// the file specified. Here I specified a directory,
// the default file that Node will try to look at
// first when you specify a directory is the index.js
// file. Follow the path here and you will find one.
const app = require('./app');