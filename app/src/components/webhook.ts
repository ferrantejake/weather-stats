import { WeatherData } from './weather';

export function sendData(data: WeatherData) {
    // use npm request module to make a POST request
    // to a test web endpoint. Use the RequestBin site
    // to make a new endpoint and make calls to it.
    // https://requestb.in/
}
